{"prompt":"Easy 01 (selection): Why is this IF condition a bug?","code":"lives ← 3\nIF (lives ← 0) {\n  DISPLAY(\"Game over\")\n}","choices":["It uses assignment (←) instead of comparison (=) in the IF condition","The IF statement should check for lives > 0 instead of lives = 0","The variable lives must be decreased before the IF statement runs","The DISPLAY message should show the number of lives instead of \"Game over\""],"correctIndex":0}
{"prompt":"Easy 02 (selection): This code should display \"Teen\" for ages 13–19. What is the bug?","code":"age ← 15\nIF (age ≥ 13 AND age ≥ 19) {\n  DISPLAY(\"Teen\")\n}","choices":["The second comparison should be age ≤ 19, not age ≥ 19","The logical operator AND should be replaced with OR to widen the range","The first comparison should strictly be age > 13 instead of age ≥ 13","The IF condition should only use one comparison, not two separate ones"],"correctIndex":0}
{"prompt":"Easy 03 (selection): The program should display \"High score\" when score is at least 100. What is wrong?","code":"score ← 100\nIF (score > 100) {\n  DISPLAY(\"High score\")\n}","choices":["The condition excludes 100; it should use ≥ instead of >","The DISPLAY command needs to be placed before the IF statement","The variable score must be reset to 0 immediately after the IF block","The IF condition should compare score to 0 instead of comparing to 100"],"correctIndex":0}
{"prompt":"Easy 04 (selection): This code should display \"Too cold\" when temp is below 32. What is the logic bug?","code":"temp ← 20\nIF (temp > 32) {\n  DISPLAY(\"Too cold\")\n}","choices":["The comparison is reversed; it should be temp < 32","The IF should check if temp = 32, not if it is below 32","The message \"Too cold\" should be displayed only when temp is exactly 32","The variable temp needs to be initialized to 0 instead of 20"],"correctIndex":0}
{"prompt":"Easy 05 (selection): Why is this comparison a bug?","code":"status ← \"OK\"\nIF (status = OK) {\n  DISPLAY(\"All good\")\n}","choices":["The string OK must be in quotes on both sides of =","The condition should compare status to a numeric value, not a string","The IF statement should check status ≠ \"OK\" to detect a problem","The variable status should be assigned inside the IF block"],"correctIndex":0}
{"prompt":"Easy 06 (selection): Only one grade should be displayed. What is the bug?","code":"score ← 95\nIF (score ≥ 90) {\n  DISPLAY(\"A\")\n}\nIF (score ≥ 80) {\n  DISPLAY(\"B\")\n}","choices":["Two separate IFs can both run; the second should be in an ELSE block","The second IF should check for score < 90 instead of score ≥ 80","The first IF should use the > operator instead of ≥ when checking 90","The IF statements must be arranged in the opposite order to work"],"correctIndex":0}
{"prompt":"Easy 07 (selection): This should display \"Wrong\" only when guess is NOT equal to secret. What is the bug?","code":"guess ← 5\nsecret ← 5\nIF (guess = secret) {\n  DISPLAY(\"Wrong\")\n}","choices":["The condition should use ≠ instead of = to detect a wrong guess","The secret variable should be modified inside the IF before comparison","The guess variable needs to be reset to 0 before the comparison runs","The IF statement should check if guess > secret instead of equality"],"correctIndex":0}
{"prompt":"Easy 08 (selection): The second DISPLAY should only run when the IF is true, but it always runs. Why?","code":"temperature ← 35\nIF (temperature > 30)\n  DISPLAY(\"Hot\")\nDISPLAY(\"Wear sunscreen\")","choices":["Only the first DISPLAY is controlled by the IF; braces { } are missing around both lines","The IF statement should compare temperature ≥ 30 instead of > 30","The second DISPLAY command should be moved to the line above the IF","The IF statement requires an ELSE block before any additional DISPLAY"],"correctIndex":0}
{"prompt":"Easy 09 (selection): This should display \"Bring umbrella\" when it IS raining. What is the bug?","code":"isRaining ← TRUE\nIF (isRaining = FALSE) {\n  DISPLAY(\"Bring umbrella\")\n}","choices":["The condition is reversed; it should test isRaining = TRUE","The IF statement should compare isRaining to a string literal like \"TRUE\"","The IF condition should not use a Boolean variable, only numeric values","The DISPLAY command needs to be placed outside the IF to run correctly"],"correctIndex":0}
{"prompt":"Easy 10 (selection): Why is this IF–ELSE structure a syntax bug?","code":"score ← 90\nIF (score ≥ 90) {\n  grade ← \"A\"\nELSE {\n  grade ← \"B\"\n}\nDISPLAY(grade)","choices":["The closing } for the IF block is missing before ELSE","The ELSE block must strictly appear before the IF block in the sequence","The variable grade must be initialized to \"B\" before the IF runs","The DISPLAY command must be placed inside the ELSE block, not after it"],"correctIndex":0}
{"prompt":"Easy 11 (selection): Why is this set of IF and ELSE blocks invalid?","code":"age ← 12\nIF (age < 13) {\n  DISPLAY(\"Child\")\n}\nELSE {\n  DISPLAY(\"Teen\")\n}\nELSE {\n  DISPLAY(\"Adult\")\n}","choices":["A single IF statement can have only one ELSE block","The first ELSE block should be written as ELSE IF instead","The second ELSE block needs to be moved above the IF statement","The variable age must be at least 0 for an ELSE block to be valid"],"correctIndex":0}
{"prompt":"Easy 12 (selection): This code should display \"Junior ticket\" only for ages between 10 and 13. Why is it wrong?","code":"age ← 16\nIF (age < 13 OR age > 10) {\n  DISPLAY(\"Junior ticket\")\n}","choices":["The OR makes the condition true for many ages; AND should be used instead","The comparison operators should be age ≤ 13 and age ≥ 10","The IF statement should only check one boundary, not two","The condition should compare age to a Boolean value instead of numbers"],"correctIndex":0}
{"prompt":"Easy 13 (selection): This should display \"Access granted\" only when BOTH are TRUE. What is the bug?","code":"usernameCorrect ← TRUE\npasswordCorrect ← FALSE\nIF (usernameCorrect OR passwordCorrect) {\n  DISPLAY(\"Access granted\")\n}","choices":["It should use AND instead of OR so both must be TRUE","The IF condition should directly compare usernameCorrect to passwordCorrect","The variables used in the condition should be numbers instead of Booleans","The DISPLAY command needs to be placed in an ELSE block instead"],"correctIndex":0}
{"prompt":"Easy 14 (selection): Why is this condition a logic bug?","code":"x ← 5\nIF (x = 3 OR 4) {\n  DISPLAY(\"Match\")\n}","choices":["The IF does not compare x to 4; it should be (x = 3 OR x = 4)","The logical operator is incorrect and should use AND instead of OR","The numbers 3 and 4 must be assigned to variables before comparison","The condition should be written to compare x to TRUE: x = 3 OR x = 4 = TRUE"],"correctIndex":0}
{"prompt":"Easy 15 (selection): This should display \"Fail\" when score is below 60. What is the bug?","code":"score ← 50\nIF (score ≥ 60) {\n  DISPLAY(\"Fail\")\n}","choices":["The comparison is backwards; it should be score < 60","The IF statement should use score ≤ 60 instead of ≥ 60","The message inside the block should say \"Pass\" instead of \"Fail\"","The IF statement should compare score to 0 instead of 60"],"correctIndex":0}
{"prompt":"Easy 16 (selection): The program should display \"Even\" when n is even. Why is this wrong?","code":"n ← 4\nIF (n MOD 2 = 1) {\n  DISPLAY(\"Even\")\n}","choices":["The condition checks for odd; it should use n MOD 2 = 0","The IF statement should check n MOD 2 ≠ 1 instead","The displayed message should be changed to \"Odd\" instead of \"Even\"","The MOD operator should be dividing by 10 instead of 2"],"correctIndex":0}
{"prompt":"Easy 17 (selection): This should display \"Game over\" when lives reach 0 or less. What is the bug?","code":"lives ← 0\nIF (lives > 0) {\n  DISPLAY(\"Game over\")\n}","choices":["The condition is backwards; it displays the message only when lives are positive","The IF statement must use an ELSE clause to work correctly","The DISPLAY command needs parentheses around the string to run","Using the value 0 for lives prevents the IF from executing"],"correctIndex":0}
{"prompt":"Easy 18 (selection): Why is this use of NOT incorrect?","code":"isHungry ← TRUE\nIF (NOT isHungry = TRUE) {\n  DISPLAY(\"Eat now\")\n}","choices":["NOT should wrap the entire comparison: NOT(isHungry = TRUE)","The NOT operator should be placed after TRUE, not before isHungry","The IF statement should compare isHungry to a number instead of TRUE","The condition should simply be isHungry = FALSE without using NOT"],"correctIndex":0}
{"prompt":"Easy 19 (selection): Only one weather message should display. What is the bug?","code":"temp ← 90\nIF (temp ≥ 80) {\n  DISPLAY(\"Hot\")\n}\nELSE {\n  DISPLAY(\"Warm\")\n}\nDISPLAY(\"Weather report done\")","choices":["The last DISPLAY is outside the IF–ELSE and therefore always runs","The ELSE block should be placed before the IF block","The IF statement should use temp > 80 instead of temp ≥ 80","The code requires another ELSE block to handle the final DISPLAY"],"correctIndex":0}
{"prompt":"Easy 20 (selection): This should display \"Inside\" only when x is between 1 and 10 exclusive. Why is it wrong?","code":"x ← 5\nIF (x > 1 OR x < 10) {\n  DISPLAY(\"Inside\")\n}","choices":["It should use AND instead of OR so both comparisons must be true","The comparison operators should be x ≥ 1 and x ≤ 10","The IF condition must compare x to a Boolean value instead of numbers","The IF statement should use only one comparison, not two"],"correctIndex":0}
{"prompt":"Easy 21 (selection): This should display \"Correct\" only when answer equals secret. What is the bug?","code":"answer ← 7\nsecret ← 7\nIF (answer ≠ secret) {\n  DISPLAY(\"Correct\")\n}","choices":["The condition is reversed; it should use = instead of ≠","The IF statement should check if answer > secret","The program should display \"Wrong\" instead of \"Correct\"","The variable secret should be assigned inside the IF block"],"correctIndex":0}
{"prompt":"Easy 22 (selection): Why is this IF statement invalid AP CSP pseudocode?","code":"num ← 3\nIF num > 0 {\n  DISPLAY(\"Positive\")\n}","choices":["The condition must be inside parentheses: IF (num > 0)","The IF statement should compare num to a Boolean, not a number","The comparison operator should be num ≥ 0 instead of num > 0","The DISPLAY command needs to appear before the IF statement"],"correctIndex":0}
{"prompt":"Easy 23 (selection): This should display \"Free\" when cost is 0. Why might it fail?","code":"cost ← 0\nIF (cost) {\n  DISPLAY(\"Free\")\n}","choices":["The condition should explicitly compare cost to 0, like cost = 0","The IF statement should compare cost to TRUE instead of 0","The IF statement should check cost ≠ 0 to detect non-free items","The variable cost must be initialized to 1 instead of 0"],"correctIndex":0}
{"prompt":"Easy 24 (selection): The program works, but the structure is not ideal. Why is this considered poor style?","code":"score ← 75\nIF (score ≥ 60) {\n  DISPLAY(\"Pass\")\n}\nIF (score < 60) {\n  DISPLAY(\"Fail\")\n}","choices":["Using two separate IF statements is redundant; an IF/ELSE structure is clearer and guarantees mutual exclusivity","The first IF statement should use score > 60 instead of ≥ 60","The IF conditions are written in the wrong order","The score variable must be reset before checking the conditions"],"correctIndex":0}
{"prompt":"Easy 25 (selection): Why does this comparison fail to match the answer?","code":"answer ← \"YES\"\nIF (answer = \"yes\") {\n  DISPLAY(\"Confirmed\")\n}","choices":["The comparison is case-sensitive; \"YES\" is not equal to \"yes\"","The IF statement should strictly check answer ≠ \"yes\"","The DISPLAY message content must match the variable name","The variable answer should store a number instead of text"],"correctIndex":0}
{"prompt":"Easy 26 (selection): This should display \"Small\" when n is less than 10. What is the bug?","code":"n ← 5\nIF (n > 10) {\n  DISPLAY(\"Small\")\n}","choices":["The comparison is reversed; it should be n < 10","The IF statement should use n ≥ 10 instead of n > 10","The variable n should be initialized to 10","The message \"Small\" should display when n ≥ 10 instead"],"correctIndex":0}
{"prompt":"Easy 27 (selection): Why is this grading logic incomplete?","code":"score ← 85\nIF (score ≥ 90) {\n  DISPLAY(\"A\")\n}\nELSE {\n  IF (score ≥ 80) {\n    DISPLAY(\"B\")\n  }\n}","choices":["Scores below 80 are never handled by this code","The inner IF statement should not be placed inside the ELSE","The ELSE condition must check score < 90 instead of score ≥ 80","The IF condition for \"B\" must use score > 90"],"correctIndex":0}
{"prompt":"Easy 28 (selection): This should display \"Unlocked\" when hasKey is TRUE. What is the bug?","code":"hasKey ← TRUE\nIF (hasKey = \"TRUE\") {\n  DISPLAY(\"Unlocked\")\n}","choices":["It compares to the string \"TRUE\" instead of the Boolean value TRUE","The IF statement should check hasKey = FALSE instead","The variable hasKey should be initialized as FALSE","The DISPLAY command should be outside the IF so it always runs"],"correctIndex":0}
{"prompt":"Easy 29 (selection): This should display \"Inside\" when x is between 1 and 100 inclusive. What is the bug?","code":"x ← 50\nIF (x > 1 AND x < 100) {\n  DISPLAY(\"Inside\")\n}","choices":["The endpoints 1 and 100 are excluded; it should use ≥ and ≤","The IF condition should use OR instead of the AND operator","The comparisons should be reversed: x < 1 AND x > 100","The variable x should start at 0 instead of 50"],"correctIndex":0}
{"prompt":"Easy 30 (selection): Why is this age check backwards?","code":"age ← 10\nIF (age < 0 OR age > 200) {\n  DISPLAY(\"Valid age\")\n}","choices":["It displays \"Valid age\" only for impossible ages; the logic should be reversed","The IF condition should use AND instead of OR","The message should be changed to \"Invalid age\" for these values","The variable age should be a string instead of a number"],"correctIndex":0}
{"prompt":"Easy 31 (selection): What is wrong with this IF–ELSE–ELSE sequence?","code":"a ← 4\nb ← 4\nIF (a = b) {\n  DISPLAY(\"Equal\")\n}\nELSE {\n  DISPLAY(\"Not equal\")\n}\nELSE {\n  DISPLAY(\"Error\")\n}","choices":["A single IF chain can have only one ELSE block","The second ELSE block should be placed before the IF","The IF condition should use ≠ instead of = when comparing a and b","The DISPLAY in the first ELSE should be moved after the second ELSE"],"correctIndex":0}
{"prompt":"Easy 32 (selection): This code should display \"On\" when lightOn is TRUE. What is the bug?","code":"lightOn ← TRUE\nIF (lightOn ← TRUE) {\n  DISPLAY(\"On\")\n}","choices":["It uses assignment (←) in the condition instead of comparison (=)","The IF statement should check lightOn = FALSE instead","The message should say \"Off\" when lightOn is TRUE","The variable lightOn should be a number, not a Boolean"],"correctIndex":0}
{"prompt":"Easy 33 (selection): This should display \"Discount\" for customers 65 or older. What is wrong?","code":"age ← 65\nIF (age > 65) {\n  DISPLAY(\"Discount\")\n}","choices":["Age 65 is excluded; it should be age ≥ 65","The IF statement should check age < 65 instead","The message should display only when age = 65","The variable age should be initialized to 0"],"correctIndex":0}
{"prompt":"Easy 34 (selection): Why might this IF block never run?","code":"count ← 0\nIF (count < 0) {\n  DISPLAY(\"Negative\")\n}","choices":["count is 0, so the condition is FALSE and the body never runs","The IF statement should use count ≥ 0 instead of count < 0","The variable count should be set to -1 before the IF","The DISPLAY command should be moved outside the IF"],"correctIndex":0}
{"prompt":"Easy 35 (selection): This should display \"Match\" when color is \"red\" or \"blue\". What is the bug?","code":"color ← \"red\"\nIF (color = \"red\" AND color = \"blue\") {\n  DISPLAY(\"Match\")\n}","choices":["A value cannot be both \"red\" and \"blue\" at the same time; OR should be used instead of AND","The IF statement should compare color to a Boolean instead of strings","The code should use two separate IF statements, not one","The variable color should be initialized as \"blue\" instead of \"red\""],"correctIndex":0}
{"prompt":"Easy 36 (selection): This code should display \"Pass\" when score is at least 60 and \"Fail\" otherwise. What is the bug?","code":"score ← 60\nIF (score > 60) {\n  DISPLAY(\"Pass\")\n}\nELSE {\n  DISPLAY(\"Fail\")\n}","choices":["A score of exactly 60 is treated as \"Fail\"; the condition should use ≥ instead of >","The ELSE block should display \"Pass\" and the IF should display \"Fail\"","The IF statement should check for score < 60 instead","The variable score must start at 0 for the logic to work"],"correctIndex":0}
{"prompt":"Easy 37 (selection): The code should display \"Child\" for age < 13, \"Teen\" for 13–17, and \"Adult\" otherwise. What is wrong?","code":"age ← 15\nIF (age < 13) {\n  DISPLAY(\"Child\")\n}\nELSE IF (age > 17) {\n  DISPLAY(\"Teen\")\n}\nELSE {\n  DISPLAY(\"Adult\")\n}","choices":["The Teen condition should be age ≥ 13 AND age ≤ 17, not age > 17","The Child condition should be age ≤ 13 instead of age < 13","The ELSE block should display \"Teen\" instead of \"Adult\"","The first IF and ELSE IF blocks should be swapped"],"correctIndex":0}
{"prompt":"Easy 38 (selection): The robot should move only when both sensors are TRUE. What is the bug?","code":"frontClear ← TRUE\nbatteryOK ← FALSE\nIF (frontClear OR batteryOK) {\n  DISPLAY(\"Move\")\n}","choices":["The condition should use AND instead of OR so both must be TRUE","The variable batteryOK should be set to TRUE inside the IF","The IF statement should compare frontClear to FALSE","The condition should use NOT(frontClear) instead"],"correctIndex":0}
{"prompt":"Easy 39 (selection): This code should display \"Inside\" only when x is between 10 and 20 inclusive. What is the logic bug?","code":"x ← 25\nIF (x ≥ 10 AND x ≥ 20) {\n  DISPLAY(\"Inside\")\n}","choices":["The second comparison should be x ≤ 20, not x ≥ 20","The first comparison should be x > 10, not x ≥ 10","The logic condition should use OR instead of AND","The IF statement should compare x to a Boolean instead of numbers"],"correctIndex":0}
{"prompt":"Easy 40 (selection): The program should display \"Alarm\" when temperature is above 100 OR smokeDetected is TRUE. What is wrong?","code":"temperature ← 105\nsmokeDetected ← FALSE\nIF (temperature > 100 AND smokeDetected = TRUE) {\n  DISPLAY(\"Alarm\")\n}","choices":["The condition should use OR instead of AND","The variable smokeDetected should be compared to \"TRUE\" as a string","The IF statement should use temperature ≥ 100 instead of > 100","The DISPLAY command should be placed inside an ELSE block"],"correctIndex":0}
{"prompt":"Easy 41 (selection): The code should display \"Equal\" when a and b are both 0. What is the bug?","code":"a ← 0\nb ← 0\nIF (a = 0 OR b = 0) {\n  DISPLAY(\"Equal\")\n}","choices":["The condition should require both to be 0 using AND, not OR","The condition should check a ≠ 0 AND b ≠ 0 instead","The code should compare variable a directly to variable b","The IF statement should check for a = 1 AND b = 1"],"correctIndex":0}
{"prompt":"Easy 42 (selection): The program should display \"Safe\" only when NOT emergency. What is the bug?","code":"emergency ← TRUE\nIF (NOT emergency = TRUE) {\n  DISPLAY(\"Safe\")\n}","choices":["NOT should wrap the whole comparison: NOT(emergency = TRUE)","The IF statement should check emergency = TRUE instead","The message should be displayed in an ELSE block","The variable emergency must be set to FALSE at the start"],"correctIndex":0}
{"prompt":"Easy 43 (selection): The code should display \"Even and positive\" only when n is even AND greater than 0. What is wrong?","code":"n ← -4\nIF (n MOD 2 = 0 OR n > 0) {\n  DISPLAY(\"Even and positive\")\n}","choices":["The logical operator should be AND because the number must satisfy both criteria at once","The MOD comparison should be checked as n MOD 2 = 1","The IF statement should compare n to a Boolean value","The display message should change based on the value of n"],"correctIndex":0}
{"prompt":"Easy 44 (selection): The door should unlock only when codeEntered is TRUE and attempts ≤ 3. What is the bug?","code":"codeEntered ← TRUE\nattempts ← 4\nIF (codeEntered = TRUE AND attempts < 3) {\n  DISPLAY(\"Unlocked\")\n}","choices":["The attempts comparison should allow 3 by using ≤ instead of <","The condition should use OR instead of AND","The variable attempts should be initialized to 0","The IF statement should check attempts > 3 instead"],"correctIndex":0}
{"prompt":"Easy 45 (selection): This code should display \"Outside range\" when x is less than 1 OR greater than 10. What is wrong?","code":"x ← 0\nIF (x < 1 AND x > 10) {\n  DISPLAY(\"Outside range\")\n}","choices":["The logical operator must be OR because a number cannot satisfy both conditions simultaneously","The comparisons should be x ≤ 1 AND x ≥ 10","The IF statement should compare x to a Boolean value","The IF statement should only check if x = 0"],"correctIndex":0}
{"prompt":"Easy 46 (selection): The program should display \"Zero\" when n is exactly 0. According to the intended logic, only one condition is wrong. Which condition is incorrect?","code":"n ← 0\nIF (n < 0) {\n  DISPLAY(\"Negative\")\n}\nELSE IF (n > 0) {\n  DISPLAY(\"Positive\")\n}\nELSE IF (n ≠ 0) {\n  DISPLAY(\"Zero\")\n}","choices":["The final condition uses n ≠ 0, which can never detect zero; it must be n = 0","The first condition should use n ≤ 0 instead of n < 0","The second condition should use n ≥ 0 instead of n > 0","The ELSE IF chain must always start by checking n = 0 in the first branch"],"correctIndex":0}
{"prompt":"Easy 47 (selection): The program should display \"Discount\" if isStudent is TRUE OR age < 18. What is the bug?","code":"isStudent ← FALSE\nage ← 16\nIF (isStudent = TRUE AND age < 18) {\n  DISPLAY(\"Discount\")\n}","choices":["The logical operator should be OR to allow either condition to grant the discount","The comparison for age < 18 should be age ≤ 18","The IF statement should check age > 18","The variable isStudent should be compared to \"TRUE\" instead of TRUE"],"correctIndex":0}
{"prompt":"Easy 48 (selection): This code should display \"Login blocked\" if attempts ≥ 3. What is wrong?","code":"attempts ← 3\nIF (attempts > 3) {\n  DISPLAY(\"Login blocked\")\n}","choices":["The comparison excludes 3; it should use ≥ instead of >","The IF statement should check attempts < 3","The variable attempts should be reset to 0 inside the IF","The message should be displayed in an ELSE block"],"correctIndex":0}
{"prompt":"Easy 49 (selection): The robot should turn left when leftClear is TRUE and rightClear is FALSE. What is the bug?","code":"leftClear ← TRUE\nrightClear ← FALSE\nIF (leftClear = TRUE OR rightClear = FALSE) {\n  DISPLAY(\"Turn left\")\n}","choices":["The condition should use AND instead of OR","The comparison rightClear = FALSE should be rightClear = TRUE","The variable leftClear should be set to FALSE before the IF","The IF statement should not compare Boolean values"],"correctIndex":0}
{"prompt":"Easy 50 (selection): The code should display \"Match\" when word is either \"yes\" or \"no\". What is wrong?","code":"word ← \"yes\"\nIF (word = \"yes\" AND word = \"no\") {\n  DISPLAY(\"Match\")\n}","choices":["A string cannot equal \"yes\" and \"no\" at the same time; OR should be used instead of AND","The IF statement attempts to compare the string variable word to a Boolean value incorrectly","The variable word should be initialized to \"maybe\"","The IF statement should use ≠ instead of = for both comparisons"],"correctIndex":0}