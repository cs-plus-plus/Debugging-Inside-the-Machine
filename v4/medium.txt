{"prompt":"Medium 01 (iteration): This code is supposed to DISPLAY 1, 2, 3, 4, 5 (one per line). What is the main bug?","code":"count ← 0\nREPEAT 5 TIMES {\n  count ← count + 1\n}\nDISPLAY(count)","choices":["DISPLAY runs only once after the loop, so it shows just 5 instead of 1 through 5","count should be reset to 0 inside the loop","The REPEAT should be REPEAT 4 TIMES instead of REPEAT 5 TIMES","The DISPLAY must be inside an IF statement"],"correctIndex":0}
{"prompt":"Medium 02 (iteration): This loop should DISPLAY 1 2 3 4 5 and then stop. Why is it infinite?","code":"x ← 1\nREPEAT UNTIL (x > 5) {\n  DISPLAY(x)\n}","choices":["x is never changed in the loop, so x > 5 never becomes TRUE","The condition should be x ≥ 5 instead of x > 5","x starts at 1 instead of 0","The DISPLAY should be placed after the loop"],"correctIndex":0}
{"prompt":"Medium 03 (iteration): This loop is meant to add 3 + 2 + 1 into sum. What is the bug?","code":"sum ← 0\nn ← 3\nREPEAT n TIMES {\n  sum ← sum + n\n  n ← n - 1\n}\nDISPLAY(sum)","choices":["REPEAT n TIMES should not change n inside the loop body","sum should be displayed inside the loop, not after it","sum must start at 3, not 0, when n is 3","The REPEAT should be REPEAT 2 TIMES instead of REPEAT n TIMES"],"correctIndex":0}
{"prompt":"Medium 04 (iteration): This loop should display every element of numbers. What is the off-by-one bug?","code":"index ← 1\nREPEAT UNTIL (index = LENGTH(numbers)) {\n  DISPLAY(numbers[index])\n  index ← index + 1\n}","choices":["It stops when index = LENGTH(numbers), so the last element is never shown","The loop should stop only when index > LENGTH(numbers)","index should be reset to 1 after each DISPLAY","The loop should start with index ← 2 instead of 1"],"correctIndex":0}
{"prompt":"Medium 05 (iteration): This loop was supposed to run many times, but it stops immediately. What is wrong?","code":"counter ← 1\nREPEAT UNTIL (counter ≥ 1) {\n  DISPLAY(\"Looping\")\n  counter ← counter + 1\n}","choices":["The UNTIL condition is already TRUE before the loop starts","The condition should be counter > 10 instead of counter ≥ 1","counter should be decreased instead of increased","counter must start at 0 instead of 1"],"correctIndex":0}
{"prompt":"Medium 06 (iteration): This FOR EACH loop is supposed to DISPLAY each value. Why is this a bug?","code":"numbers ← [1, 2, 3]\nFOR EACH value IN numbers {\n  DISPLAY(numbers[index])\n}","choices":["It should DISPLAY(value); index is never defined here","index should start at 1 before the loop begins","The code should use REPEAT 3 TIMES instead of FOR EACH","numbers must contain strings, not numbers"],"correctIndex":0}
{"prompt":"Medium 07 (iteration): This code should sum all items in numbers. Why is it a logic bug?","code":"total ← 0\nnumbers ← [1, 2, 3, 4]\nindex ← 1\nREPEAT 4 TIMES {\n  total ← total + numbers[index]\n}\nDISPLAY(total)","choices":["index never changes, so the same element is added every time","REPEAT 4 TIMES should be REPEAT UNTIL (index > LENGTH(numbers))","total must start at numbers[1] instead of 0","index should start at 0 instead of 1 for lists"],"correctIndex":0}
{"prompt":"Medium 08 (iteration): This loop should display numbers from 10 down to 1. Why is it infinite?","code":"n ← 10\nREPEAT UNTIL (n = 0) {\n  DISPLAY(n)\n  n ← n + 1\n}","choices":["n goes up instead of down, so it never reaches 0","The condition should be n < 0 instead of n = 0","n starts at the wrong value; it should start at 0","DISPLAY should be called after the REPEAT, not inside it"],"correctIndex":0}
{"prompt":"Medium 09 (iteration): This code is supposed to sum the numbers 1 through 5. What is the bug?","code":"sum ← 0\nk ← 1\nREPEAT UNTIL (k = 5) {\n  sum ← sum + k\n  k ← k + 1\n}\nDISPLAY(sum)","choices":["It stops when k = 5, so 5 is not added to sum","k should increase by 2 each time to reach 5 faster","The loop should stop when k ≥ 5 instead of when k = 5","sum should be displayed inside the loop, not after it"],"correctIndex":0}
{"prompt":"Medium 10 (iteration): This loop should print all elements of items, but it crashes. Why?","code":"items ← [\"a\", \"b\", \"c\"]\nindex ← 1\nREPEAT 4 TIMES {\n  DISPLAY(items[index])\n  index ← index + 1\n}","choices":["It runs 4 times even though there are only 3 elements","index should be decreased instead of increased","items should contain numbers, not strings","REPEAT 4 TIMES should be REPEAT UNTIL (index > LENGTH(items))"],"correctIndex":0}
{"prompt":"Medium 11 (iteration): This should DISPLAY every odd number from 1 to 9. What is the bug?","code":"n ← 1\nREPEAT UNTIL (n > 9) {\n  DISPLAY(n)\n}","choices":["n is never updated in the loop, so it never ends","n should be decreased instead of increased","The condition should be n ≥ 9 instead of n > 9","DISPLAY(n) should be the last line outside the loop"],"correctIndex":0}
{"prompt":"Medium 12 (iteration): This code should count how many zeros are in data. What is wrong?","code":"data ← [0, 1, 0, 2]\ncount ← 0\nFOR EACH value IN data {\n  IF (value = 0) {\n    count ← count + 1\n  }\n}\nDISPLAY(value)","choices":["The program displays value at the end instead of count","count should be reset to 0 inside the loop","The IF condition should be value ≠ 0 instead","The code should use REPEAT UNTIL instead of FOR EACH"],"correctIndex":0}
{"prompt":"Medium 13 (iteration): This loop should multiply all numbers in nums into product. What is the bug?","code":"nums ← [2, 3, 4]\nproduct ← 1\nindex ← 1\nREPEAT UNTIL (index > LENGTH(nums)) {\n  product ← product * nums[index]\n}","choices":["index never changes, so the loop never terminates","product should start at 0 instead of 1","The condition should be index ≥ LENGTH(nums) instead of index > LENGTH(nums)","The code should use FOR EACH instead of REPEAT UNTIL"],"correctIndex":0}
{"prompt":"Medium 14 (iteration): This code should double each number in numbers. Why doesn’t the list change?","code":"numbers ← [1, 2, 3]\nFOR EACH num IN numbers {\n  num ← num * 2\n}\nDISPLAY(numbers[1])","choices":["Changing num does not update the list element itself","numbers[1] is the wrong element to display","FOR EACH only works when the list is empty","num must be reset to 0 on each iteration"],"correctIndex":0}
{"prompt":"Medium 15 (iteration): This loop should DISPLAY the first 3 elements of data. What is wrong?","code":"data ← [5, 6, 7, 8]\nindex ← 1\nREPEAT 3 TIMES {\n  index ← index + 1\n  DISPLAY(data[index])\n}","choices":["index is incremented before DISPLAY, so the first element is skipped","index should be decreased instead of increased","REPEAT 3 TIMES should be REPEAT 4 TIMES","data should contain exactly 3 elements instead of 4"],"correctIndex":0}
{"prompt":"Medium 16 (iteration): This code should find the first negative number and then stop. Why is it buggy?","code":"values ← [3, -1, 4]\nindex ← 1\nREPEAT UNTIL (index > LENGTH(values)) {\n  IF (values[index] < 0) {\n    DISPLAY(values[index])\n  }\n  index ← index + 1\n}","choices":["It never exits early; it should stop the loop once a negative is found","The condition should be index ≥ LENGTH(values) instead of index > LENGTH(values)","The IF should check values[index] > 0 instead","index should start at 0 instead of 1"],"correctIndex":0}
{"prompt":"Medium 17 (iteration): This loop should DISPLAY all names in students, but it skips some. Why?","code":"students ← [\"Ana\", \"Ben\", \"Cia\", \"Dan\"]\nindex ← 1\nREPEAT UNTIL (index ≥ LENGTH(students)) {\n  DISPLAY(students[index])\n  index ← index + 2\n}","choices":["index jumps by 2, so some names are never reached","The condition should be index > LENGTH(students) instead of index ≥ LENGTH(students)","students should be sorted before the loop","index should be reset to 1 inside the loop"],"correctIndex":0}
{"prompt":"Medium 18 (iteration): This code is supposed to DISPLAY each letter in word. Why does it break?","code":"word ← [\"C\", \"O\", \"D\", \"E\"]\nindex ← 1\nREPEAT UNTIL (index > LENGTH(word)) {\n  DISPLAY(word[index])\n  index ← index + 1\n}\nDISPLAY(word[index])","choices":["After the loop, index is past the end of the list, so the final DISPLAY is out of range","The REPEAT UNTIL condition should be index ≥ LENGTH(word)","Letters cannot be stored in lists","index should not be changed inside the loop"],"correctIndex":0}
{"prompt":"Medium 19 (iteration): This loop should DISPLAY \"Tick\" 10 times. Why does it show only 9 times?","code":"i ← 1\nREPEAT UNTIL (i = 10) {\n  DISPLAY(\"Tick\")\n  i ← i + 1\n}","choices":["It stops when i reaches 10, so it only runs for i = 1 through 9","The condition should be i > 10 instead of i = 10","i should be decreased instead of increased","REPEAT UNTIL cannot be used with equality"],"correctIndex":0}
{"prompt":"Medium 20 (iteration): This should sum only the even numbers in nums. What is the bug?","code":"nums ← [1, 2, 3, 4]\nsum ← 0\nFOR EACH n IN nums {\n  IF (n MOD 2 = 0) {\n    sum ← sum + 1\n  }\n}\nDISPLAY(sum)","choices":["It adds 1 instead of adding n, so it counts evens instead of totaling them","The IF should check n MOD 2 ≠ 0 to find even numbers","sum should start at 1 instead of 0","MOD cannot be used with list values"],"correctIndex":0}
{"prompt":"Medium 21 (iteration): This loop should build a list of 5 zeros. What is wrong?","code":"zeros ← []\ncount ← 0\nREPEAT 5 TIMES {\n  zeros[count] ← 0\n  count ← count + 1\n}","choices":["It writes to index 0 first, but AP CSP lists normally start at 1","count should be reset to 0 each time","REPEAT 5 TIMES should be REPEAT UNTIL (count = 5)","zeros must be pre-filled with placeholder values"],"correctIndex":0}
{"prompt":"Medium 22 (iteration): This FOR EACH loop should display each value. Why is it wrong?","code":"data ← [10, 20, 30]\nFOR EACH value IN data {\n  DISPLAY(data)\n}","choices":["It prints the whole list each time instead of the loop variable","value should be increased after each DISPLAY","The list data must contain strings, not numbers","FOR EACH should be replaced with REPEAT LENGTH(data) TIMES"],"correctIndex":0}
{"prompt":"Medium 23 (iteration): This loop should DISPLAY the first 4 numbers of nums. Why does it miss the last one?","code":"nums ← [5, 6, 7, 8]\nindex ← 1\nREPEAT UNTIL (index ≥ LENGTH(nums)) {\n  DISPLAY(nums[index])\n  index ← index + 1\n}","choices":["It stops when index reaches LENGTH(nums), so the last item is never displayed","The condition should be index > LENGTH(nums)","index should start at 0 instead of 1","REPEAT UNTIL should be replaced with REPEAT 4 TIMES"],"correctIndex":0}
{"prompt":"Medium 24 (iteration): This code should count to 3 and then display \"Done\". Why does the loop never run?","code":"x ← 1\nREPEAT UNTIL (x < 3) {\n  DISPLAY(x)\n  x ← x + 1\n}\nDISPLAY(\"Done\")","choices":["The UNTIL condition is TRUE at the start, so the body is skipped","The condition should be x > 3 instead of x < 3","x must start at 0 instead of 1","DISPLAY(\"Done\") should be inside the loop"],"correctIndex":0}
{"prompt":"Medium 25 (iteration): This loop aims to remove all zeros, but it skips some. Why?","code":"nums ← [0, 1, 0, 2]\nindex ← 1\nREPEAT UNTIL (index > LENGTH(nums)) {\n  IF (nums[index] = 0) {\n    REMOVE(nums, index)\n  }\n  index ← index + 1\n}","choices":["After a removal, the next value slides into the same index but index still increases","The condition should be index ≥ LENGTH(nums)","REMOVE should happen after index ← index + 1","The IF should check nums[index] ≠ 0 instead"],"correctIndex":0}
{"prompt":"Medium 26 (iteration): This code should DISPLAY the squares of 1, 2, and 3. What is the bug?","code":"i ← 1\nREPEAT 3 TIMES {\n  DISPLAY(i * i)\n}","choices":["i never changes, so it always shows 1","It should DISPLAY(i + i) instead of i * i","REPEAT 3 TIMES should be REPEAT 2 TIMES","i should start at 0 instead of 1"],"correctIndex":0}
{"prompt":"Medium 27 (iteration): This code should count from 1 to 5. Why does it start from 2?","code":"i ← 1\nREPEAT 5 TIMES {\n  i ← i + 1\n  DISPLAY(i)\n}","choices":["i is incremented before DISPLAY, so the first output is 2","The loop should use REPEAT 4 TIMES instead of 5","The increment should be i ← i - 1","i should start at 0 instead of 1"],"correctIndex":0}
{"prompt":"Medium 28 (iteration): This loop should sum values until it reaches a negative. What is the bug?","code":"nums ← [2, 4, -1, 5]\nsum ← 0\nindex ← 1\nREPEAT UNTIL (nums[index] < 0) {\n  sum ← sum + nums[index]\n  index ← index + 1\n}\nDISPLAY(sum)","choices":["If the very first element is negative, the body never runs at all","The condition should check index instead of nums[index]","The condition should be nums[index] ≤ 0 instead of nums[index] < 0","sum should be reset to 0 inside the loop"],"correctIndex":0}
{"prompt":"Medium 29 (iteration): This FOR EACH loop is supposed to copy list a into list b. Why does b stay empty?","code":"a ← [1, 2, 3]\nb ← []\nFOR EACH value IN b {\n  APPEND(b, value)\n}\nDISPLAY(LENGTH(b))","choices":["It iterates over b, which is empty, instead of over a","APPEND should insert at a specific index instead of the end","The code should use REMOVE instead of APPEND","LENGTH(b) should be compared to 3 instead of displayed"],"correctIndex":0}
{"prompt":"Medium 30 (iteration): This code should DISPLAY all elements of nums exactly once. Why is one extra DISPLAY wrong?","code":"nums ← [1, 2, 3]\nindex ← 1\nREPEAT UNTIL (index > LENGTH(nums)) {\n  DISPLAY(nums[index])\n  index ← index + 1\n}\nDISPLAY(nums[index])","choices":["After the loop, index is out of range, so the final DISPLAY is invalid","The loop condition should be index ≥ LENGTH(nums)","nums should contain 4 elements instead of 3","index should start at 0"],"correctIndex":0}
{"prompt":"Medium 31 (iteration): This loop should DISPLAY 5, 4, 3, 2, 1. Why is it wrong?","code":"n ← 5\nREPEAT UNTIL (n < 1) {\n  DISPLAY(n)\n  n ← n + 1\n}","choices":["n is increased instead of decreased, so n < 1 never becomes TRUE","The condition should be n ≤ 1 instead of n < 1","n should start at 1 instead of 5","DISPLAY(n) should be outside the loop"],"correctIndex":0}
{"prompt":"Medium 32 (iteration): This code should count how many elements are bigger than 10. What is the bug?","code":"values ← [5, 12, 20]\ncount ← 0\nFOR EACH v IN values {\n  IF (v > 10) {\n    count ← count + 1\n  }\n}\nDISPLAY(v)","choices":["It prints v at the end instead of count","The IF should use v ≥ 10 instead of v > 10","count should be reset to 0 inside the loop","v should be increased after each iteration"],"correctIndex":0}
{"prompt":"Medium 33 (iteration): This code should add up 1 + 2 + 3 + 4. Why is it wrong?","code":"n ← 4\nsum ← 0\nREPEAT n TIMES {\n  sum ← sum + n\n}\nDISPLAY(sum)","choices":["It always adds n, instead of a counter that changes each time","sum should be displayed on each iteration instead of at the end","REPEAT n TIMES should be REPEAT 3 TIMES","n should start at 1 instead of 4"],"correctIndex":0}
{"prompt":"Medium 34 (iteration): This code should DISPLAY each character in message, but it never stops. Why?","code":"message ← [\"H\", \"I\"]\nindex ← 1\nREPEAT UNTIL (index = LENGTH(message)) {\n  DISPLAY(message[index])\n}","choices":["index never changes, so the loop condition never becomes TRUE","The condition should be index > LENGTH(message)","index should start at 0","The list message is too short"],"correctIndex":0}
{"prompt":"Medium 35 (iteration): This loop should run exactly 10 times. What is the subtle bug risk?","code":"i ← 0\nREPEAT UNTIL (i ≥ 10) {\n  i ← i + 1\n  DISPLAY(i)\n}","choices":["If the start value or condition changes slightly, it can easily run 9 or 11 times instead of 10","REPEAT UNTIL cannot use ≥ in AP CSP pseudocode","DISPLAY(i) must be outside the loop","i cannot be initialized to 0"],"correctIndex":0}
{"prompt":"Medium 36 (iteration): This loop should DISPLAY each element of nums exactly once. Why does it skip the first element?","code":"nums ← [4, 5, 6, 7]\nindex ← 1\nREPEAT UNTIL (index > LENGTH(nums)) {\n  index ← index + 1\n  DISPLAY(nums[index])\n}","choices":["index changes before DISPLAY, so nums[1] is never printed","The condition should be index ≥ LENGTH(nums) instead of index > LENGTH(nums)","nums should contain 5 values instead of 4","DISPLAY(nums[index]) should be moved above the REPEAT"],"correctIndex":0}
{"prompt":"Medium 37 (iteration): This code should stop summing when total reaches at least 10. What is the bug?","code":"values ← [3, 4, 5]\nindex ← 1\ntotal ← 0\nREPEAT UNTIL (total > 10) {\n  total ← total + values[index]\n  index ← index + 1\n}\nDISPLAY(total)","choices":["It uses total > 10, so it keeps going until total passes 10, not when it first equals 10","The condition should check index instead of total","total should be reset to 0 at the end of each iteration","index should start at 0"],"correctIndex":0}
{"prompt":"Medium 38 (iteration): This loop should DISPLAY 2, 4, 6, 8, 10. Why is it wrong?","code":"n ← 2\nREPEAT UNTIL (n > 10) {\n  DISPLAY(n)\n  n ← n + 1\n}","choices":["n goes up by 1, so both even and odd numbers are printed","The condition should be n ≥ 10 instead of n > 10","n should start at 0","DISPLAY(n) should be after the loop"],"correctIndex":0}
{"prompt":"Medium 39 (iteration): This code should count how many negative numbers are in nums. Why is it buggy?","code":"nums ← [1, -2, 3, -4]\ncount ← 0\nindex ← 1\nREPEAT UNTIL (index ≥ LENGTH(nums)) {\n  IF (nums[index] < 0) {\n    count ← count + 1\n  }\n  index ← index + 1\n}\nDISPLAY(count)","choices":["It stops when index = LENGTH(nums), so the last item is never checked","The IF should use nums[index] ≤ 0","count should start at 1 instead of 0","index should be decreased instead of increased"],"correctIndex":0}
{"prompt":"Medium 40 (iteration): This loop should DISPLAY each character in the list chars, but it shows the last character twice. Why?","code":"chars ← [\"A\", \"B\", \"C\"]\nindex ← 1\nREPEAT UNTIL (index > LENGTH(chars)) {\n  DISPLAY(chars[index])\n  index ← index + 1\n}\nDISPLAY(chars[LENGTH(chars)])","choices":["The extra DISPLAY after the loop repeats the final element","The loop condition should be index ≥ LENGTH(chars)","chars should contain 4 elements instead of 3","index should not be changed inside the loop"],"correctIndex":0}
{"prompt":"Medium 41 (iteration): This code should DISPLAY \"Found\" once if nums contains 7. Why might it DISPLAY \"Found\" multiple times?","code":"nums ← [7, 1, 7]\nindex ← 1\nREPEAT UNTIL (index > LENGTH(nums)) {\n  IF (nums[index] = 7) {\n    DISPLAY(\"Found\")\n  }\n  index ← index + 1\n}","choices":["The loop never exits early; every 7 in the list triggers a \"Found\"","The IF condition should be nums[index] ≠ 7","The loop must stop as soon as index reaches 2","nums should not contain duplicates"],"correctIndex":0}
{"prompt":"Medium 42 (iteration): This loop should DISPLAY each element and its index. What is the bug?","code":"vals ← [10, 20, 30]\nindex ← 1\nREPEAT UNTIL (index > LENGTH(vals)) {\n  DISPLAY(index)\n  DISPLAY(vals[1])\n  index ← index + 1\n}","choices":["It always shows vals[1] instead of vals[index]","index should start at 0","The condition should be index ≥ LENGTH(vals)","DISPLAY(index) should be outside the loop"],"correctIndex":0}
{"prompt":"Medium 43 (iteration): This code should build a running sum and DISPLAY each partial sum. Why does it DISPLAY the same sum twice?","code":"nums ← [2, 4, 6]\nindex ← 1\nsum ← 0\nREPEAT UNTIL (index > LENGTH(nums)) {\n  DISPLAY(sum)\n  sum ← sum + nums[index]\n  index ← index + 1\n}","choices":["It prints sum before adding nums[index], so each value is shifted by one step","sum should start at nums[1] instead of 0","The condition should be index ≥ LENGTH(nums)","The list nums is too short"],"correctIndex":0}
{"prompt":"Medium 44 (iteration): This loop should multiply all numbers together, but the result is always 0. Why?","code":"nums ← [2, 3, 4]\nproduct ← 0\nFOR EACH n IN nums {\n  product ← product * n\n}\nDISPLAY(product)","choices":["Starting product at 0 makes every multiplication stay 0","The FOR EACH should be a REPEAT loop","The list nums should start with 0 as the first element","DISPLAY(product) should be inside the loop"],"correctIndex":0}
{"prompt":"Medium 45 (iteration): This code should DISPLAY the values at even positions (2, 4, 6...). What is the bug?","code":"nums ← [5, 6, 7, 8]\nindex ← 1\nREPEAT UNTIL (index > LENGTH(nums)) {\n  IF (index MOD 2 = 0) {\n    DISPLAY(nums[index])\n  }\n  index ← index + 2\n}","choices":["index already skips by 2, so checking MOD 2 is unnecessary and can hide mistakes","The IF should use index MOD 2 ≠ 0","The list nums is too short","index must start at 0"],"correctIndex":0}
{"prompt":"Medium 46 (iteration): This loop should DISPLAY numbers 1 through 5. Why does it never DISPLAY 5?","code":"i ← 1\nREPEAT UNTIL (i ≥ 5) {\n  DISPLAY(i)\n  i ← i + 1\n}","choices":["When i reaches 5, the UNTIL condition stops the loop before printing 5","The condition should be i < 5 instead of i ≥ 5","i should start at 0 instead of 1","DISPLAY(i) should be after i ← i + 1"],"correctIndex":0}
{"prompt":"Medium 47 (iteration): This code should count how many numbers are at least 5. What is the bug?","code":"nums ← [3, 5, 7]\ncount ← 0\nFOR EACH n IN nums {\n  IF (n > 5) {\n    count ← count + 1\n  }\n}\nDISPLAY(count)","choices":["It excludes n = 5; the condition should be n ≥ 5","count should start at 1","The loop should break after finding the first value ≥ 5","FOR EACH cannot be used with numeric values"],"correctIndex":0}
{"prompt":"Medium 48 (iteration): This loop should DISPLAY \"Hi\" exactly 3 times. Why does it DISPLAY it 4 times?","code":"i ← 0\nREPEAT UNTIL (i > 3) {\n  DISPLAY(\"Hi\")\n  i ← i + 1\n}","choices":["The loop runs for i = 0, 1, 2, 3, so it executes 4 times","The condition should be i ≥ 3","i should start at 1 instead of 0","DISPLAY(\"Hi\") should be outside the loop"],"correctIndex":0}
{"prompt":"Medium 49 (iteration): This code should sum only positive values. Why is 0 included in the sum?","code":"vals ← [0, 1, -2, 3]\nsum ← 0\nFOR EACH v IN vals {\n  IF (v ≥ 0) {\n    sum ← sum + v\n  }\n}\nDISPLAY(sum)","choices":["Using v ≥ 0 includes 0; it should check v > 0 instead","sum should start at vals[1]","0 should be removed from vals before looping","FOR EACH should be replaced with REPEAT UNTIL"],"correctIndex":0}
{"prompt":"Medium 50 (iteration): This loop should DISPLAY the index of the first zero and then stop. Why does it keep running?","code":"nums ← [1, 0, 2]\nindex ← 1\nREPEAT UNTIL (index > LENGTH(nums)) {\n  IF (nums[index] = 0) {\n    DISPLAY(index)\n  }\n  index ← index + 1\n}","choices":["It never exits after finding a zero; the loop just continues","The IF should check nums[index] ≠ 0 instead","index should not be incremented after finding a zero","The condition should be index ≥ LENGTH(nums)"],"correctIndex":0}
{"prompt":"Medium 51 (iteration): This code should DISPLAY the list in reverse order. Why does it not show the first element?","code":"nums ← [9, 8, 7]\nindex ← LENGTH(nums)\nREPEAT UNTIL (index = 1) {\n  DISPLAY(nums[index])\n  index ← index - 1\n}","choices":["The loop stops when index reaches 1, so nums[1] is never printed","index should start at 1","The condition should be index < 1","nums should contain 4 elements"],"correctIndex":0}
{"prompt":"Medium 52 (iteration): This loop should double each list element and store it back. Why does it not change the list?","code":"nums ← [1, 2, 3]\nFOR EACH n IN nums {\n  APPEND(nums, n * 2)\n}\nDISPLAY(nums[1])","choices":["It appends new values instead of replacing the old ones, so originals stay the same","APPEND should be used outside the loop","nums[1] is the wrong element to display","n must be reset to 0 each iteration"],"correctIndex":0}
{"prompt":"Medium 53 (iteration): This code should count how many values are less than 10. Why is the count too large?","code":"vals ← [5, 10, 15]\ncount ← 0\nFOR EACH v IN vals {\n  IF (v ≤ 10) {\n    count ← count + 1\n  }\n}\nDISPLAY(count)","choices":["The condition counts 10 as well; it should use v < 10","count should be decremented instead of incremented","The loop should skip the first value","vals should not include 10"],"correctIndex":0}
{"prompt":"Medium 54 (iteration): This code should copy all values less than 5 from a into b. Why is b empty?","code":"a ← [1, 2, 6]\nb ← []\nindex ← 1\nREPEAT UNTIL (index > LENGTH(a)) {\n  IF (a[index] < 5) {\n    APPEND(a, a[index])\n  }\n  index ← index + 1\n}\nDISPLAY(LENGTH(b))","choices":["The program appends into a instead of b, so b never changes","The condition should be a[index] ≥ 5","index should start at 0","APPEND should be used before the IF"],"correctIndex":0}
{"prompt":"Medium 55 (iteration): This loop should DISPLAY the sum after each addition. Why does the last DISPLAY show an incorrect total?","code":"nums ← [1, 2, 3]\nindex ← 1\nsum ← 0\nREPEAT UNTIL (index > LENGTH(nums)) {\n  sum ← sum + nums[index]\n  DISPLAY(sum)\n  index ← index + 1\n}\nDISPLAY(sum + 1)","choices":["The final DISPLAY adds 1 to sum, so the last value shown is not the real total","DISPLAY(sum) should be outside the loop","sum should start at nums[1]","The condition should be index ≥ LENGTH(nums)"],"correctIndex":0}
{"prompt":"Medium 56 (iteration): This loop should DISPLAY each value times 2. Why does it run into an error?","code":"vals ← [2, 4, 6]\nindex ← 1\nREPEAT UNTIL (index > LENGTH(vals)) {\n  DISPLAY(vals[index] * 2)\n}\nindex ← index + 1","choices":["index never changes inside the loop, so the UNTIL condition is never met","The condition should be index ≥ LENGTH(vals)","vals should start at 0","DISPLAY cannot multiply inside its argument"],"correctIndex":0}
{"prompt":"Medium 57 (iteration): This code should sum all elements of data except the last one. Why does it include the last element?","code":"data ← [1, 2, 3, 4]\nindex ← 1\nsum ← 0\nREPEAT UNTIL (index > LENGTH(data)) {\n  sum ← sum + data[index]\n  index ← index + 1\n}\nDISPLAY(sum)","choices":["The loop goes until index > LENGTH(data), so it adds every element","index should start at 0","The condition should be index ≥ LENGTH(data)","sum should be reset each iteration"],"correctIndex":0}
{"prompt":"Medium 58 (iteration): This loop should DISPLAY values at indexes 1, 3, and 5. Why does it crash?","code":"vals ← [10, 20, 30, 40, 50]\nindex ← 1\nREPEAT UNTIL (index ≥ LENGTH(vals)) {\n  DISPLAY(vals[index])\n  index ← index + 2\n}","choices":["When index gets close to the end, index + 2 can move beyond the list length","index should be incremented by 1 instead of 2","The condition should be index > LENGTH(vals)","vals must have an even number of elements"],"correctIndex":0}
{"prompt":"Medium 59 (iteration): This code should count the number of times 3 appears. Why does it always show 0?","code":"nums ← [3, 1, 3]\ncount ← 0\nFOR EACH n IN nums {\n  IF (n ≠ 3) {\n    count ← count + 1\n  }\n}\nDISPLAY(count)","choices":["The condition is reversed; it increments count when n is not 3","count should start at 1","The loop should break after the first 3","FOR EACH should be replaced with REPEAT UNTIL"],"correctIndex":0}
{"prompt":"Medium 60 (iteration): This loop should DISPLAY the maximum value in vals. Why is the result wrong?","code":"vals ← [5, 9, 2]\nmax ← 0\nFOR EACH v IN vals {\n  IF (v < max) {\n    max ← v\n  }\n}\nDISPLAY(max)","choices":["The comparison is backwards; it updates max when v is smaller, not larger","max should start at vals[1]","The IF should be outside the loop","The list vals should be sorted first"],"correctIndex":0}
{"prompt":"Medium 61 (iteration): This code should DISPLAY only the first 2 elements. Why does it show all 3?","code":"vals ← [4, 5, 6]\nindex ← 1\nREPEAT UNTIL (index > 2) {\n  DISPLAY(vals[index])\n  index ← index + 1\n}","choices":["Once index becomes 3, the body has already executed for vals[3]","The condition should be index ≥ 2","The list vals is too long","index should start at 0"],"correctIndex":0}
{"prompt":"Medium 62 (iteration): This loop should DISPLAY only positive numbers. Why does it also DISPLAY negatives?","code":"vals ← [-1, 2, -3]\nFOR EACH v IN vals {\n  IF (v > 0) {\n    DISPLAY(v)\n  }\n  ELSE {\n    DISPLAY(v)\n  }\n}","choices":["The ELSE also DISPLAYs v, so both positive and negative numbers appear","The IF condition should be v ≥ 0","The ELSE block should be removed entirely","The list vals must not contain negatives"],"correctIndex":0}
{"prompt":"Medium 63 (iteration): This code should stop as soon as it finds a value greater than 100. Why does it keep going?","code":"vals ← [50, 200, 150]\nindex ← 1\nREPEAT UNTIL (index > LENGTH(vals)) {\n  IF (vals[index] > 100) {\n    DISPLAY(\"Found big value\")\n  }\n  index ← index + 1\n}","choices":["Nothing exits the loop after a match, so later values are still checked","The IF condition should be vals[index] ≥ 100","The index update should be inside the IF","The list vals must be sorted first"],"correctIndex":0}
{"prompt":"Medium 64 (iteration): This loop should DISPLAY values while they are less than 10. Why does it crash?","code":"vals ← [5, 9, 12]\nindex ← 1\nREPEAT UNTIL (vals[index] ≥ 10) {\n  DISPLAY(vals[index])\n  index ← index + 1\n}\nDISPLAY(vals[index])","choices":["After the loop, index might be past the list end, so the final DISPLAY can be out of range","The condition should be vals[index] > 10","index should start at 0","DISPLAY(vals[index]) must be inside an IF"],"correctIndex":0}
{"prompt":"Medium 65 (iteration): This code should DISPLAY the average of all items. Why is the average too large?","code":"vals ← [2, 4, 6]\nsum ← 0\ncount ← 0\nFOR EACH v IN vals {\n  sum ← sum + v\n}\ncount ← LENGTH(vals) - 1\nDISPLAY(sum / count)","choices":["It divides by LENGTH(vals) - 1 instead of by the full length","sum should be reset each iteration","The loop should stop one element early","The IF should wrap the DISPLAY"],"correctIndex":0}
{"prompt":"Medium 66 (iteration): This loop should DISPLAY \"Odd\" for odd numbers and skip even ones. Why does it also DISPLAY even numbers?","code":"nums ← [1, 2, 3]\nFOR EACH n IN nums {\n  IF (n MOD 2 = 1) {\n    DISPLAY(\"Odd\")\n  }\n  DISPLAY(n)\n}","choices":["DISPLAY(n) always runs, so even values still get printed","The IF condition is wrong; it should be n MOD 2 = 0","n must start at 0","FOR EACH must include an ELSE block"],"correctIndex":0}
{"prompt":"Medium 67 (iteration): This code should DISPLAY the smallest value. Why does it always DISPLAY the last value instead?","code":"vals ← [9, 3, 5]\nmin ← vals[1]\nindex ← 1\nREPEAT UNTIL (index > LENGTH(vals)) {\n  min ← vals[index]\n  index ← index + 1\n}\nDISPLAY(min)","choices":["min is overwritten every time and never compared, so it ends up as the final element","The condition should be index ≥ LENGTH(vals)","min should start at 0","The loop should not update index"],"correctIndex":0}
{"prompt":"Medium 68 (iteration): This code should count how many elements are equal to 0 or 1. Why does it miss some?","code":"vals ← [0, 1, 2, 1]\ncount ← 0\nFOR EACH v IN vals {\n  IF (v = 0) {\n    count ← count + 1\n  }\n  ELSE IF (v = 1) {\n    count ← count + 1\n  }\n}\nDISPLAY(count)","choices":["With more conditions later, ELSE IF would block other checks; here separate IFs would be safer","The IF conditions should be swapped","count should start at 1","The loop should end after the first match"],"correctIndex":0}
{"prompt":"Medium 69 (iteration): This loop should DISPLAY each value, but stop when a value is negative. Why does it still DISPLAY the negative value?","code":"vals ← [4, 3, -1, 2]\nindex ← 1\nREPEAT UNTIL (index > LENGTH(vals) OR vals[index] < 0) {\n  DISPLAY(vals[index])\n  index ← index + 1\n}","choices":["The negative value is printed before the condition is checked again","The OR should be AND","index should start at 0","vals must not contain negative values"],"correctIndex":0}
{"prompt":"Medium 70 (iteration): This code should DISPLAY all values and then \"Done\". Why does it never DISPLAY \"Done\"?","code":"vals ← [1, 2, 3]\nindex ← 1\nREPEAT UNTIL (index ≥ LENGTH(vals)) {\n  DISPLAY(vals[index])\n  index ← index + 1\n  IF (index ≥ LENGTH(vals)) {\n    index ← 1\n  }\n}\nDISPLAY(\"Done\")","choices":["index is reset to 1 inside the loop, so the UNTIL condition is never left true","The IF inside the loop should be removed","The condition should be index > LENGTH(vals)","\"Done\" must be DISPLAYed inside the loop"],"correctIndex":0}
{"prompt":"Medium 71 (iteration): This loop should fill a list with the numbers 1 through 4. Why is the first element 0?","code":"vals ← []\nindex ← 0\nREPEAT 4 TIMES {\n  vals[index] ← index\n  index ← index + 1\n}\nDISPLAY(vals[0])","choices":["The code stores index itself, so the first stored value is 0 instead of 1","REPEAT 4 TIMES should be REPEAT 5 TIMES","index should start at 1","vals must not be empty before the loop"],"correctIndex":0}
{"prompt":"Medium 72 (iteration): This loop should DISPLAY each element only once. Why does it get stuck on the first element?","code":"vals ← [3, 4, 5]\nindex ← 1\nREPEAT UNTIL (index > LENGTH(vals)) {\n  DISPLAY(vals[index])\n  IF (vals[index] = 3) {\n    index ← 1\n  }\n  index ← index + 1\n}","choices":["Whenever vals[index] is 3, index is reset, so it keeps returning to the first element","The IF should check vals[index] ≠ 3","index should start at 0","LENGTH(vals) is wrong"],"correctIndex":0}
{"prompt":"Medium 73 (iteration): This code should compute the factorial of n (n!). Why is it incorrect for n = 4?","code":"n ← 4\nresult ← 1\nk ← 1\nREPEAT UNTIL (k > n) {\n  result ← result * k\n  k ← k + 2\n}\nDISPLAY(result)","choices":["k jumps by 2, so it multiplies 1, 3 and skips 2 and 4","result should start at 0","The condition should be k ≥ n","The loop should not use k at all"],"correctIndex":0}
{"prompt":"Medium 74 (iteration): This code should count how many times the value changes between neighbors. Why does it miss some changes?","code":"vals ← [1, 2, 2, 3]\nindex ← 1\ncount ← 0\nREPEAT UNTIL (index > LENGTH(vals)) {\n  IF (vals[index] ≠ vals[index + 1]) {\n    count ← count + 1\n  }\n  index ← index + 2\n}\nDISPLAY(count)","choices":["index increases by 2, so it skips some neighbor pairs entirely","The IF condition should be vals[index] = vals[index + 1]","count should start at 1","The condition should be index ≥ LENGTH(vals)"],"correctIndex":0}
{"prompt":"Medium 75 (iteration): This code should DISPLAY the sum of the first n positive integers. Why does it give the wrong answer?","code":"n ← 5\nsum ← 0\nk ← 1\nREPEAT UNTIL (k > n) {\n  sum ← sum + n\n  k ← k + 1\n}\nDISPLAY(sum)","choices":["It adds n every time instead of k, so it computes n × n","k should start at 0","The condition should be k ≥ n","sum should start at 1 instead of 0"],"correctIndex":0}
